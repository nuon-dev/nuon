# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

.DS_Store
._.DS_Store
**/.DS_Store
**/._.DS_Store

# dependencies
/node_modules
pnpm-lock.yaml

# Local env files (NEVER commit secrets) — templates with .example are allowed
.env
.env.*
!.env.example
!.env.*.example

# DB connection config (contains credentials)
ormconfig.js

# database backups
/backups
*.sql
*.sql.gz